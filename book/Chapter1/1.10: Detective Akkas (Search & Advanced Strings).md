Chapter 1.10: Detective Akkas (Search & Advanced Strings)
Topic: Search Methods (IndexOf, Includes), Regex Matching, Unicode & Legacy Methods. Story: Finding Hidden Sugar in Food Labels.

আক্কাস এখন সুপারশপে। বিস্কুটের প্যাকেটে উপাদান লেখা:

```javascript
let ingredients = "Flour, Water, Sugar, Salt, SugarSyrup";
```

সে ডিটেকটিভের মতো খুঁজবে—এর মধ্যে "Sugar" আছে কি না।

1.10.1 Positional Search (কোথায় আছে?)
🕵️♂️ দৃশ্যপট (Scene)
আক্কাস মিয়ার ডায়েট চার্ট রেডি। কিন্তু তার সন্দেহ হচ্ছে, ডাক্তার কি গোপনে কোথাও "চিনি" (Sugar) ঢুকিয়ে দিয়েছে? আক্কাস বলল, "খালি চোখে খুঁজলে হবে না, জাভাস্ক্রিপ্ট দিয়ে পজিশনাল সার্চ করতে হবে। চিনি যেখানেই লুকিয়ে থাকুক, তার ইন্ডেক্স নম্বর (Index Number) আমি বের করবই!"

ধরি, আক্কাসের ডায়েট চার্ট স্ট্রিংটি এমন:

```javascript
let dietChart = "Rice, Sugar, Chicken, Bread, Sugar, Water";
```

১. indexOf("Sugar") - প্রথম চোর ধরা 🔍
আক্কাস জানতে চায়, প্রথম চিনিটা কত নম্বর পজিশনে আছে? indexOf() মেথডটি বাম দিক থেকে খোঁজা শুরু করে এবং প্রথম যেটাকে পায়, তার ইন্ডেক্স রিটার্ন করে।

👨💻 কোড:

```javascript
let pos1 = dietChart.indexOf("Sugar");
console.log(pos1); // 6 — প্রথম Sugar এর ইনডেক্স
```

আক্কাসের নোট: যদি চিনি না থাকত? তাহলে আউটপুট আসত -1। অর্থাৎ, "ভাই, এই জিনিস লিস্টে নাই!"
২. lastIndexOf("Sugar") - শেষের চোর ধরা 🔙
আক্কাস দেখল প্রথম চিনি ধরা পড়েছে। কিন্তু সে চালাক, সে চেক করতে চায় তালিকার শেষে আবার চিনি লুকিয়ে আছে কিনা। lastIndexOf() মেথডটি ডান দিক থেকে (বা শেষ থেকে) খোঁজা শুরু করে।

👨💻 কোড:

```javascript
let pos2 = dietChart.lastIndexOf("Sugar");
console.log(pos2); // 29 — শেষের Sugar এর ইনডেক্স
```

৩. search(/sugar/i) - স্মার্ট সার্চ (Regex) 🧠
আক্কাস দেখল, মাঝেমধ্যে ডাক্তার চালাকি করে "Sugar" এর বদলে ছোট হাতের "sugar" লিখে দেয়। indexOf কেস-সেনসিটিভ (Case Sensitive), তাই সে "sugar" আর "Sugar" কে এক মনে করে না।

এখানেই search() মেথড এবং Regular Expression (Regex) এর কামাল! /sugar/i লিখে দিলে সে ছোট-বড় হাতের তফাৎ ভুলে গিয়ে খুঁজে বের করবে। (i মানে ignore case)।

👨💻 কোড:

```javascript
let text = "No sugar added"; // 'sugar' ছোট হাতের
let pos3 = text.search(/Sugar/i);
console.log(pos3); // 3
```

👉 সাবধানতা: search() মেথডটি পাওয়ারফুল হলেও, এটি indexOf এর মতো "দ্বিতীয় আর্গুমেন্ট" (কোথা থেকে খোঁজা শুরু করব) নেয় না। তাই সাধারণ খোঁজাখুঁজির জন্য indexOf ই আক্কাসের প্রথম পছন্দ!

1.10.2 Boolean Search (আছে কি নাই?)
আক্কাস পজিশন চায় না, শুধু সত্য/মিথ্যা (True/False) চায়।

```javascript
ingredients.includes("Sugar"); // true
ingredients.startsWith("Flour"); // true
ingredients.endsWith("Salt"); // false (ends with "SugarSyrup")
```

১.১০.৩ Advanced Matching (রেজেক্স দিয়ে শিকার) 🎯
🕵️♂️ দৃশ্যপট (Scene)
আক্কাস মিয়া এখন আর সাধারণ খোঁজায় সন্তুষ্ট না। সে এখন স্নাইপার হতে চায়। সে এমন সব শব্দ খুঁজে বের করতে চায়, যাদের শেষে "ain" আছে (যেমন: Rain, Pain, Spain)।

সাধারণ indexOf দিয়ে এটা সম্ভব না। তাই আক্কাস তার গোপন অস্ত্র Regular Expression (Regex) বের করল।

আমাদের টেস্ট বাক্য:

```javascript
let text = "The rain in SPAIN stays mainly in the plain";
```

১. match(/regex/) - একবারে সব শিকার 📦
আক্কাস বলল, "ভাই, একটা একটা করে খুঁজব না। যেখানে যেখানে 'ain' আছে, সবগুলোকে ধরে একটা বস্তায় (Array) ভরে দাও।"

এর জন্য সে ব্যবহার করল match() মেথড এবং সাথে g (global) এবং i (case-insensitive) ফ্ল্যাগ।

👨💻 কোড:

```javascript
// /ain/gi -> 'ain' খোঁজ, সব জায়গায় (g), ছোট-বড় হাত বাদ দিয়ে (i)
let results = text.match(/ain/gi);
console.log(results); // ["ain", "AIN", "ain", "ain"]
```

আক্কাসের সতর্কতা: যদি কোনো মিল না পায়, তবে এটি null রিটার্ন করে (খালি অ্যারে নয়)। তাই সাবধানে!

২. matchAll() - আধুনিক লুপ শিকারী 🔄
match() মেথড শুধু শব্দগুলো দেয়, কিন্তু তাদের বিস্তারিত (যেমন: কোন শব্দটা কত নম্বর ইন্ডেক্সে আছে) দেয় না। বিস্তারিত তথ্যের জন্য এবং মেমোরি বাঁচাতে আক্কাস ব্যবহার করে matchAll()। এটি একটি Iterator রিটার্ন করে, যার ওপর লুপ চালানো যায়।

👨💻 কোড:

```javascript
let iterator = text.matchAll(/ain/gi);
for (let match of iterator) {
  console.log(`পাওয়া গেছে: ${match[0]}, পজিশন: ${match.index}`);
}
// আউটপুট:
// পাওয়া গেছে: ain, পজিশন: 5
// পাওয়া গেছে: AIN, পজিশন: 14
// ...এবং বাকিগুলো।
```

➡️ আক্কাসের শেষ কথা: "সাধারণ খোঁজার জন্য includes বা indexOf ভালো। কিন্তু যখন প্যাটার্ন (Pattern) ধরে খুঁজতে হবে, তখন Regex ই হলো আসল বস!"

1.10.4 Comparison & Unicode (একটু অ্যাডভান্সড)
localeCompare(): ডিকশনারি অনুযায়ী দুইটা শব্দ তুলনা করে। সর্টিং-এর সময় লাগে.

```javascript
let a = "Apple";
let b = "Banana";
console.log(a.localeCompare(b)); // -1 (কারণ A আগে আসে)
```

isWellFormed(): স্ট্রিংটা কি ঠিকঠাক আছে নাকি ভেঙে গেছে?.
toWellFormed(): ভাঙা ইমোজি বা স্ট্রিং রিপেয়ার করে দেয়.
valueOf(): স্ট্রিং অবজেক্টের আসল প্রিমিটিভ ভ্যালু দেয়.
fromCharCode(): নাম্বার থেকে অক্ষর বানায়

১.১০.৫ HTML Wrapper Methods (জাদুঘরের সামগ্রী) 🦖
🏛️ দৃশ্যপট (Scene)
আক্কাস মিয়া পুরনো জাভাস্ক্রিপ্ট বই ঘাঁটতে ঘাঁটতে কিছু অদ্ভুত মেথড খুঁজে পেল। সে দেখল, কোনো স্ট্রিংয়ের সাথে .bold() লাগিয়ে দিলেই সেটা মোটা হয়ে যাচ্ছে!

আক্কাস আনন্দে নেচে উঠল:

"ইউরেকা! আর কষ্ট করে HTML ট্যাগ বা CSS লিখতে হবে না। জাভাস্ক্রিপ্ট দিয়েই সব ডিজাইন করে ফেলব!"
কিন্তু তার সিনিয়র ভাইয়া তাকে থামিয়ে দিয়ে বললেন:

"আক্কাস, থাম! এগুলো কোড না, এগুলো ডাইনোসর। এগুলো জাদুঘরে সাজিয়ে রাখার জিনিস, প্রজেক্টে ব্যবহারের জন্য না!"
🚫 এগুলো কী এবং কেন ব্যবহার করবেন না?
জাভাস্ক্রিপ্টের শুরুর দিকে (৯০-এর দশকে) যখন CSS অতটা শক্তিশালী ছিল না, তখন স্ট্রিংকে সরাসরি HTML ট্যাগ দিয়ে মুড়িয়ে দেওয়ার জন্য এই মেথডগুলো তৈরি করা হয়েছিল।

কিন্তু এখন HTML (গঠন), CSS (সাজসজ্জা) এবং JS (লজিক) আলাদা রাখা হয়। এই মেথডগুলো এখন Deprecated (বাতিল ঘোষিত)।

👨💻 আক্কাসের জাদুঘর (deprecated methods):

```javascript
let txt = "Hello World";

// ১. মোটা করা
console.log(txt.bold()); // "<b>Hello World</b>"

// ২. বাঁকা করা
console.log(txt.italics()); // "<i>Hello World</i>"

// ৩. কেটে দেওয়া
console.log(txt.strike()); // "<strike>Hello World</strike>"

// ৪. লিঙ্ক বানানো
console.log(txt.link("https://google.com")); // "<a href='https://google.com'>Hello World</a>"
```

এছাড়াও আছে: big(), small(), sub(), sup(), এবং কুখ্যাত blink() (যেটা টেক্সটকে ব্লিঙ্ক করাত, যা এখন কোনো ব্রাউজার সাপোর্ট করে না)।

✅ তাহলে মডার্ন উপায় কী?
আক্কাস এখন স্মার্ট হয়েছে। সে জানে স্টাইল করার জন্য CSS ব্যবহার করতে হয়, জাভাস্ক্রিপ্টের স্ট্রিং মেথড নয়।

সঠিক পদ্ধতি (DOM & CSS):

```javascript
// ❌ ভুল পদ্ধতি (আক্কাস এটা ঘৃণা করে)
document.body.innerHTML = txt.bold();

// ✅ সঠিক পদ্ধতি (আক্কাস এটা পছন্দ করে)
let p = document.createElement("p");
p.innerText = "Hello World";
p.style.fontWeight = "bold"; // CSS দিয়ে স্টাইল
document.body.appendChild(p);
```

উপদেশ: এই মেথডগুলো ইন্টারভিউতে জিজ্ঞেস করলে শুধু বলবেন— "এগুলো লিগ্যাসি কোড (Legacy Code), মডার্ন ডেভেলপমেন্টে এগুলো ব্যবহার করা ব্যাড প্র্যাকটিস।"

🚀 Chapter Summary: স্ট্রিং মাস্টারি
অভিনন্দন! আপনি এবং আক্কাস এখন স্ট্রিং-এর বস। আমরা স্ট্রিং-এর প্রতিটি কোণা (Methods, Search, Regex, Legacy) কভার করেছি।
