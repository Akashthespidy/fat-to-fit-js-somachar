## ১.৫ JS Operators (গণিত এবং তুলনা)

### 🧮 দৃশ্যপট (Scene)

আক্কাস ভেরিয়েবল বা পাত্র বানানো শিখেছে। কিন্তু শুধু পাত্র সাজিয়ে রাখলে তো হবে না, ভেতরে হিসাব-নিকাশ করতে হবে।

**আক্কাস:**

> "সারাদিন যা গিললাম, সেটার হিসাব যদি জাভাস্ক্রিপ্ট না রাখে, তবে আর কোডিং শিখে লাভ কী?"

জাভাস্ক্রিপ্টকে ক্যালকুলেটর হিসেবে ব্যবহার করার জন্য আমাদের দরকার **Operators**। চলুন আক্কাসের সারাদিনের খাওয়ার হিসাব দিয়ে অপারেটরগুলো চিনে নিই।

---

### ১. Assignment Operator (`=`)

এটি গণিতের "সমান সমান" নয়। কোডিংয়ে এর মানে হলো— **ডানপাশের মান বামপাশের পাত্রে রাখা।**

**🍳 দিনের শুরু:**
আক্কাসের দিন শুরু হলো, এখনো কিছু খায়নি। তাই ক্যালরি ০।

```javascript
let dailyCalories = 0; // দিন শুরু হলো ০ ক্যালরি দিয়ে
```

---

### ২. Arithmetic Operators (যোগ, বিয়োগ, গুণ, ভাগ)

সাধারণ গণিতের মতোই জাভাস্ক্রিপ্ট যোগ-বিয়োগ করতে পারে।

**➕ Addition Operator (`+`):**
সকালে আক্কাস ২টা ডিম আর ১টা পরোটা খেল। মোট ৪৫০ ক্যালরি পেটে গেল। এখন আগের ক্যালরির সাথে এটা যোগ করতে হবে।

```javascript
dailyCalories = dailyCalories + 450;
console.log("সকালের নাস্তার পর মোট ক্যালরি:", dailyCalories);
// আউটপুট: 450
```

**✖️ Multiplication Operator (`*`):**
বিকালে বন্ধুদের পাল্লায় পড়ে আক্কাস ৩টা সিঙ্গারা খেয়ে ফেলল! এক একটা সিঙ্গারায় ১৫০ ক্যালরি। এখন কি সে বারবার ১৫০+১৫০+১৫০ যোগ করবে? না! সে গুণ করবে।

```javascript
let singaraCount = 3;
let caloriesPerSingara = 150;

// গুণ করে মোট স্ন্যাকস ক্যালরি বের করা হলো
let snackCalories = singaraCount * caloriesPerSingara;

console.log("শুধু সিঙ্গারার ক্যালরি:", snackCalories);
// ৩ * ১৫০ = ৪৫০ ক্যালরি (সর্বনাশ!)

// এবার এটা মোট ক্যালরিন সাথে যোগ করে দিই
dailyCalories = dailyCalories + snackCalories;
console.log("বিকালে মোট ক্যালরি:", dailyCalories);
// ৪৫০ (সকাল) + ৪৫০ (বিকাল) = ৯০০ ক্যালরি
```

---

### ৩. Comparison Operators (তুলনা করা)

দিনশেষে আক্কাস ভয়ে ভয়ে চেক করবে সে কি তার ডায়েট লিমিট ক্রস করেছে কিনা। এটার উত্তর সবসময় `true` (হ্যাঁ) অথবা `false` (না) আসবে।

**Comparison Operator (`>`):**
আক্কাসের লিমিট ২০০০ ক্যালরি। ধরুন, রাতে বিরিয়ানি খেয়ে তার মোট ক্যালরি হয়ে গেল ২৫০০।

```javascript
let limit = 2000; // সর্বোচ্চ সীমা
let totalIntake = 2500; // আজকের মোট খাওয়া (হায়রে কপাল!)

// ২৫০০ কি ২০০০ এর চেয়ে বড়? (সত্যি নাকি মিথ্যা?)
let isOverEating = totalIntake > limit;

console.log("আমি কি বেশি খেয়েছি?:", isOverEating);
// Output: true (দুঃখজনক হলেও সত্য!)
```

---

### 📌 একনজরে আক্কাসের অপারেটর চার্ট

| অপারেটর | নাম            | কাজ              | উদাহরণ           |
| ------- | -------------- | ---------------- | ---------------- |
| `=`     | Assignment     | মান সেট করা      | `x = 10`         |
| `+`     | Addition       | যোগ করা          | `10 + 5`         |
| `-`     | Subtraction    | বিয়োগ করা        | `10 - 2`         |
| `*`     | Multiplication | গুণ করা          | `10 * 2`         |
| `/`     | Division       | ভাগ করা          | `10 / 2`         |
| `>`     | Greater Than   | তুলনা (বড় কিনা)  | `10 > 5` (true)  |
| `<`     | Less Than      | তুলনা (ছোট কিনা) | `10 < 5` (false) |

---

**➡️ পরের ধাপ:**
আক্কাস দেখল `isOverEating = true`। মানে সে বেশি খেয়ে ফেলেছে। এখন জাভাস্ক্রিপ্ট তাকে বকা দেবে নাকি মাফ করে দেবে? এই সিদ্ধান্ত নেওয়ার জন্য আমাদের শিখতে হবে **Conditions (if-else)**।
